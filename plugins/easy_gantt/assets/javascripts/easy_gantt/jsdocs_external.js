/**
 * Created by hosekp on 11/14/16.
 */
(function () {
  /**
   * Do not include this file - it is just for suppressing JSDocs warnings
   */
  window.showModal =function(){};
  jQuery.fn.dialog=function(){};
  /**
   *
   * @param {String} type
   * @param {String} message
   * @param {number} delay
   */
  window.showFlashMessage=function(type, message, delay){};
  /**
   *
   * @param {String} htmlId
   */
  window.fillFormTextAreaFromCKEditor = function(htmlId){};

  jQuery.Deferred.prototype.done = function (func) {

  };
  window._ = window._ || {};

  ysy.settings.parentIssueDates = true;
  ysy.settings.milestonePush = true;
  ysy.settings.easyRedmine = true;
  ysy.settings.fixedRelations = true;
  /**
   * @param {String} name
   * @param {Function} body
   */
  window.describe = window.fdescribe = window.xdescribe = function (name, body) {

  };
  /**
   * @param {String} name
   * @param {Function} body
   */
  window.fit = window.xit = window.it = function (name, body) {

  };
});
