$("#other-expenses").html('<%= j(render :partial => 'easy_money_other_expenses/inline_show', :locals => {:project => @project, :sum_other_expenses => @entity.easy_money(@current_easy_currency_code).sum_other_expenses(@project.easy_money_settings.expected_count_price.to_sym), :entity_type => @entity_type, :entity_id => @entity_id, :easy_currency_code => @current_easy_currency_code}) %>');

renderOtherProfit();
