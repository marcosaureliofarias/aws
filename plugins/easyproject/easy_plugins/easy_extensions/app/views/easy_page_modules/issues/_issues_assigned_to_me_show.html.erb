<% filter = easy_page_modules_data[:only_assigned] ? :assigned_to_id : :participant_id %>
<%= render :partial => 'easy_page_modules/easy_query_show', :locals => local_assigns.merge({ :entities => easy_page_modules_data[:assigned_issues], :options => { :disable_sort => true, :hascontextmenu => true } }) %>

<% content_for :header_tags do %>
  <%= auto_discovery_link_tag(:atom, issues_path(:set_filter => 1, filter => user.id, :format => 'atom', :key => User.current.rss_key), {:title => l(:label_assigned_to_me_issues)}) %>
<% end %>
