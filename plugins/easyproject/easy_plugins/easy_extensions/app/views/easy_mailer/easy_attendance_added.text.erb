<%= l(:'easy_attendance.mail_added.body_top', :user => @user.name, :activity => @easy_attendance.easy_attendance_activity.name, :from => format_time(@from), :to => format_time(@to)) %>
<%= link_to(l(:'easy_attendance.mail_added.body_link'), {:controller => 'easy_attendances', :action => 'index', :set_filter => '1', :user_id => @user, :only_path => false}) %>