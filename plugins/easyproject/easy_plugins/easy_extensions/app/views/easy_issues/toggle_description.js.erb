$("#<%= params[:element] %>").html("<%= j(text_area_tag('issue[description]', @issue.description,
        :cols => 60,
        :rows => (@issue.description.blank? ? 10 : [[10, @issue.description.length / 50].max, 100].min),
        :class => 'wiki-edit',
        :no_label => true) + wikitoolbar_for('issue_description').to_s ) %>");

$("#description_toggler").remove();