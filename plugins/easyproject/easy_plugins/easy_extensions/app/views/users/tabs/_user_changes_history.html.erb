<% if @journals.present? && (User.current.admin? || @user == User.current )%>
  <%= easy_journal_render_history(@journals, {collapsible: false, entity: @user, default_button_state: false}) -%>
<% end %>
