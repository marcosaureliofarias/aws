<h3 class="title"><%= l(:label_add_projects) %></h3>

<%= form_for :membership, :remote => true, :url => user_memberships_path(@principal), :method => :post , :html => {:id => 'memberships_form'} do |f| %>
  <%= render :partial => 'principal_memberships/new_form' %>
<% end %>
