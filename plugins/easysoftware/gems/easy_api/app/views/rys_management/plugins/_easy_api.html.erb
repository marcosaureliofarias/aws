<%
 # This setting is available on /rys_management/easy_api/edit
 # All values are stored in EasySettings
 # Each key have own record
%>

<p>
  <% # This value is accesible via `EasyApi.setting(:value)` %>
  <% #                          or `EasySetting.value(:easy_api_value)` %>
  <%= form.label :value, l('field_value') %>
  <%= form.text_field :value %>
</p>
