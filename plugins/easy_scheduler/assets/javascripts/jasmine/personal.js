jasmineHelper.lock("meetings");
describe("Personal with mocked data", function () {
  // beforeAll(function () {
  //   this.scheduler = easyScheduler.scheduler;
  //   this.helper = easyScheduler.tests;
  // });
  //beforeEach(jasmineHelper.initPageMatchers);
  it("should load", function () {
    expect(true).toBeTruthy();
  });
});
